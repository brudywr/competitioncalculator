package org.stas.demo.calc;

/**
 *  Points can be calculated differently so this interface covers different algirithms of point calculations
 */
interface PointCalculator {

    int calculate(double rawResult);
}
